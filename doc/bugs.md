
-[DONE] olen dealer, mutta en näe toimintanappeja, vain fold to any bet tai (auto) check.
-[DONE] muck or show doesn't work. Even if I say show -> it doesn't show the cards.
-[DONE] Create table -nappi ei navigoi watching-näkymään. Juurisyy: Railway-palvelimella oli vanha single-table-koodi ilman multi-table lobbyä. Korjattu: 1) deployattu uusin koodi Railwaylle, 2) lisätty Socket.IO ack-callback create table -flowiin, 3) lisätty connection status -indikaattori (vihreä/punainen pallo) lobbynäkymään.
-[DONE] Pöydän muoto ei saa muuttua ikkunan koon mukaan. Pöydän tulee säilyttää kiinteä aspect ratio riippumatta selainikkunan koosta. Korjattu: PokerTable käyttää kiinteitä virtuaalidimensioita (900×550) ja contain-mode scale-transformia molemmissa näkymissä (TV + puhelin).
-[DONE] Disconnected pelaaja ei voi refreshin jälkeen palata istumaan samaan pöytään. Korjattu: serveri säilyttää pelaajan paikan 60s disconnectin jälkeen, reconnectToken-tunnistus sessionStoragessa, automaattinen reconnect sivun latautuessa.
-[DONE] Pelaaja ei voi valita omaa istuintaan pelipöytään istuttaessa. Korjattu: addPlayer hyväksyy valinnaisen seatIndex-parametrin. Tyhjät paikat näyttävät hoverin aikana pumppaavan "Sit In" -tekstin vihreällä pohjalla ja glow-efektillä. Klikkaus avaa buy-in-dialogin valitulle paikalle.
-[DONE] Run It Twice: toinen board vain ilmestyy eikä sillä ole animaatiota. Ensimmäinen board tulee hyvin, mutta toisen pitää tulla samalla tavalla animoituna. Kokemuksen pitää olla yhtenäinen sekä RIT-käsissä että normaaleissa käsissä. Juurisyy: PokerTable.tsx renderöi board 2 extra -kortit staattisina CardComponent-elementteinä, kun board 1 käyttää CommunityCards-komponenttia (slide-in + 3D flip). Korjattu: board 2 extra käyttää nyt CommunityCards-komponenttia (initialCount=0 → kaikki kortit animoituvat).
-[DONE] Flaky test: `integration-50-hands.test.ts` > "should handle all-in scenarios correctly across 50 hands" — expects allInHands > 0 but with random bot decisions sometimes no one goes all-in in 50 hands. Korjattu: poistettu ei-deterministinen assertion, säilytetty error-free 50-hand assertion.
-[DONE] TV-näkymässä pelipöytä on sijoitettu niin ylös, että dealer-nappi leikkautuu puoliksi ylälaidasta, kun pelaaja istuu ylärivin paikalla (erityisesti seat 5). Juurisyy: Seat 5 y-positio on 3% ja overflow-hidden leikkaa ylimenevän osan. Korjattu: Ylärivin seat-positiot siirretty alemmaksi (seat 5: y:3→8%, seats 4/6: y:10→12%).
-[DONE] Palvelimen uudelleenkäynnistyksen jälkeen puhelinnäkymä jää "Reconnecting..." -tilaan eikä pääse lobbyyn. Juurisyy: Reconnect-handler lähetti S2C_PLAYER.ERROR eikä RECONNECT_FAILED kun pöytää ei löytynyt. Client kuunteli vain RECONNECT_FAILED-eventtiä, joten reconnecting-tila ei koskaan nollautunut. Korjattu: molemmat virhetapaukset ("Table not found", "Table ID required") lähettävät nyt RECONNECT_FAILED.
-[DONE] Ylhäällä istuvan pelaajan kortit peittävät stackin numeron ja nimikyltin (Bug #12, #20). Juurisyy: PlayerSeat.tsx:n korttien absoluuttinen ankkuripiste oli kiinteästi avatarin keskellä (top: 40px), ja cardOffset työnsi ylärivin pelaajien (seats 4–6) kortteja alaspäin nimilaatan päälle. Korjattu: kun cardOffset.y > 20 (kortit menevät alaspäin), ankkuripiste siirretään nimilaatan alapuolelle (top: 135px) ja y-offset nollataan.
-[DONE] Dealer-nappi on liian pieni ja menee avatarin kanssa päällekkäin (Bug #21). Juurisyy: Dealer-nappi oli 24x24px ja sijoitettu kiinteällä +42px/-16px offsetilla paikan keskipisteestä, mikä aiheutti päällekkäisyyden 80px avatarin kanssa ja ylitti taulukon reunan oikeilla paikoilla. Korjattu: nappi kasvatettu 32x32px:iin ja sijoitetaan nyt 52px päähän paikan keskipisteestä pöydän keskustaa kohti, joten se on aina avatarin ulkopuolella eikä mene yli reunan.
-[DONE] Toimintonappulat (check, fold, raise) vilkkuvat hetken näkyviin kun pelaaja painaa check ja seuraava community-kortti tulee (Bug #23). Juurisyy: Pelaajan painaessa check, palvelin prosessoi tapahtumien (player_acted, street_dealt, player_turn) välillä viiveitä animaatioita varten, mutta asiakkaan privateState (isMyTurn, availableActions) päivittyi vasta player_turn-tapahtuman käsittelyssä. Tänä aikana vanha privateState näytti edelleen nappeja. Korjattu: GameScreen piilottaa napit optimistisesti heti kun pelaaja toimii (actionSentForTurn-lippu), ja palauttaa ne kun palvelin lähettää uuden privateState-päivityksen.
- WebXR 3D-näkymä ei auennut Meta Quest 3:lla. Todennäköinen syy: WebXR vaatii HTTPS-yhteyden, mutta mkcert CA-sertifikaatti ei ole asennettu, joten dev-palvelin toimii HTTP:llä. Quest-selain ei raportoi WebXR-tukea HTTP:n yli → `useXRDetection` palauttaa `supportsVR: false` → normaali 2D-näkymä renderöityy. Korjaus: asenna mkcert CA (`~/.vite-plugin-mkcert/mkcert -install`) ja ota mkcert()-plugin käyttöön vite.config.ts:ssä.
-[DONE] Edellisen käden potti jää pöytään kun kaikki pelaajat paitsi yksi painoivat sit out (Bug #10). Juurisyy: scheduleNextHand() nollasi currentPots-tilan vain epäsuorasti startNewHand()-kutsun kautta. Jos pelaajia ei ollut tarpeeksi seuraavaan käteen, startNewHand() ei koskaan kutsuttu, jolloin vanhat potit jäivät getTableState()-palautusarvoon. Korjattu: hand-visual-tila (pots, communityCards, secondBoard, showdownEntries, handEngine) nollataan scheduleNextHand():ssa heti kun phase vaihtuu waiting_for_players-tilaan.
-[DONE] Sit Out + Leave Table: pelaajan avatar jää pöytään ja varaa paikan (Bug #9). Juurisyy: leaveTable() tarkisti phase === 'hand_in_progress' ja lykäsi poiston pendingRemovals-jonoon, jos pelaaja oli currentHandPlayers-kartassa isFolded: false. Mutta currentHandPlayers sisältää vanhoja kopioita (shallow copy hand_started-eventistä), jotka päivittyvät vasta player_acted-eventin käsittelyssä jonosta. Jos pelaaja foldasi ja painoi heti sit out + leave, currentHandPlayers ei ollut vielä päivittynyt, ja pelaajaa ei poistettu. Korjattu: sitting_out-pelaaja ohittaa aina pendingRemovals-tarkistuksen ja poistetaan heti, koska sitting_out-status tarkoittaa ettei pelaajalla ole aktiivista osallistumista käteen.
-[DONE] Kortit lentävät pöydän alalaidasta jaettaessa, vaikka pakka näkyy pöydän keskellä. Juurisyy: startDealAnimations() käytti dealerin istuimen sijaintia (esim. seat 0 = y:92%) korttien animaation lähtöpisteenä, mutta visuaalinen DeckShuffleAnimation renderöityy kohdassa {x:50%, y:42%}. Korjattu: lisätty DECK_POSITION-vakio (50%, 42%), jota käyttävät sekä DeckShuffleAnimation että korttien jakoanimaatio. Pakan sijainti voidaan nyt muuttaa yhdestä paikasta.
-[DONE] Flaky testit (havaittu 2026-07-03 konttitestiajurin verifioinnissa, toistuu myös hostilla): `dealer-action-buttons.test.ts` > "during cards_dealt, nobody has isMyTurn=true" ja `empty-table-cleanup.test.ts` > "removes a connected player from pendingRemovals after hand completes" failasivat. Juurisyy (dealer-action-buttons, nykykoodilla deterministinen faili): testi indeksoi PRIVATE_STATE-emitit positiolla (`states[1]`), mutta commit fed3f20 lisäsi handleSitIn:iin ylimääräisen PRIVATE_STATE-emitin sitting_out-pelaajille, joten sock-2:n indeksi 1 oli sit-in-tila (0 korttia) eikä cards_dealt-snapshot. Korjattu: testi valitsee cards_dealt-snapshotin semanttisesti (viimeisin emit ennen timereiden ajoa) ja assertoi isMyTurn === false koko emit-virrasta. Juurisyy (empty-table-cleanup, ~50 % faili): ensimmäisen käden dealeri arvotaan Math.randomilla, ja heads-upissa dealeri toimii ensin preflopissa; kun Alice (seat 0) arvottiin dealeriksi, Bobin fold oli hiljaa ohitettu out-of-turn-toiminto (HandEngine ignoraa vuoron ulkopuoliset), käsi ei koskaan päättynyt eikä processPendingRemovals ajanut. Korjattu: testi pinnaa Math.randomin (vi.spyOn → 0.9) niin että Bob on aina dealeri/SB. Tuotekoodin invariantit olivat kunnossa molemmissa — kyse oli testien vanhentuneista/piilo-oletuksista, ei tuotebugeista.
-[DONE] Leave Table -nappi puuttuu kun pelaaja on ainoa pöydässä. Juurisyy: GameScreen näytti Leave Table -napin vain kun `isSittingOut === true`, mutta ensimmäisen pelaajan status on `'waiting'` (ei `'sitting_out'`). Kun käsi ei ole käynnissä, pelaaja ei näe Leave Table -nappia eikä pääse poistumaan pöydästä. Korjattu: Leave Table näkyy nyt kun `isSittingOut || !isHandActive` — eli aina kun käsi ei ole käynnissä tai pelaaja on sitting out.
-[DONE] Bad beat -animaatio ei ollut "täysin hyvä": (a) räjähdys/teksti laukesi 0 ms showdownin jälkeen eli korttien porrastetun paljastuksen päälle, joten tulos spoilattiin ennen paljastusta; (b) kolme eri elinaikaa eivät synkronoineet — isot CSS-animaatiot (flash/text) loppuivat 2.5 s kohdalla, mutta `badBeat`-tila eli 5 s → ~2.5 s "kuollutta häntää" jossa vain pikkukupla näkyi, ja pottijako tuli vasta ~6 s → ~1 s tyhjä väli; (c) sekä käsivoimakkuus- että equity-tunnistin saattoivat emittoida oman `bad_beat`-eventin samassa kädessä → client säilytti vain viimeisen → banneri välähti/korvautui. Juurisyy: `GameManager.getEventDelay` ei käsitellyt `bad_beat`-tapausta (fall-through → 0 ms) ja siirsi paljastusviiveen `hand_complete`:lle; client kovakoodasi 5000 ms:n elinajan eikä CSS käyttänyt `--ftp-anim-bad-beat`-tokenia; `HandEngine` emitoi useita `bad_beat`-eventtejä. Korjattu: (a) `bad_beat` viivästetään paljastuksen keston verran (`max(DELAY_SHOWDOWN_TO_RESULT_MS, revealDuration)`) ja `hand_complete` bad_beatin jälkeen odottaa vain `DELAY_BAD_BEAT_TO_RESULT_MS` — kokonaiskesto ennallaan, mutta räjähdys laukeaa vasta korttien paljastuttua ja potti tulee juuri kun overlay häipyy; (b) client-tila nollataan `DELAY_BAD_BEAT_TO_RESULT_MS`:n (3000 ms) kohdalla, flash+text kestävät `var(--ftp-anim-bad-beat)`-tokenin (3000 ms) ja häipyvät lopussa → ei kuollutta häntää eikä tyhjää väliä; (c) `HandEngine` emitoi täsmälleen yhden `bad_beat`-eventin (vahvin/käsivoimakkuus-pohjainen voittaa), `badBeatPlayerIds` kerää yhä kaikki. Testit: `pot-award-timing.test.ts` (uusi ajoitus) + `bad-beat.test.ts` (korkeintaan yksi bad_beat-event). Verifioitu myös visuaalisesti `/editor`-sandboxissa.
-[DONE] Animaatioajastimien cross-hand-vuoto ja siivoamattomuus: `useTableAnimations.ts`:n 15 `setTimeout`ia olivat "fire-and-forget" — effectin cleanup kutsui vain `socket.off`, ei tyhjentänyt ajastimia. Seuraukset: (a) kädestä-N "clear"-ajastin (esim. bannerien nollaus) saattoi laueta kädessä N+1 ja pyyhkiä tuoreen tilan; (b) unmountin jälkeen setState stale-hookiin; (c) peräkkäiset sivupotit (2 s välein) — aiemman potin 3 s clear-ajastin pyyhki myöhemmän potin bannerin. Korjattu: kaikki ajastimet kulkevat nyt keskitetyn `schedule(fn, ms, key?)`-avustajan kautta (ref-Map), joka tyhjennetään effectin cleanupissa JA uuden käden alussa (`onCardsDealt`); reset-semantiikan ajastimille (bannerit, potti, celebration, bad beat) annetaan `key`, jolloin myöhempi tapahtuma korvaa aiemman clear-ajastimen sen sijaan että kilpailisi sen kanssa.
-[DONE] Royal/straight flush -konfetti katkesi kesken putoamisen: partikkelien max-elinaika oli 5.5 s (delay 0–1.5 s + kesto 2.5–4 s) mutta juhla unmountattiin 4.5 s kohdalla. Korjattu: konfettien ajoitus sidottu jaettuun `CELEBRATION_DURATION_MS`-vakioon (delay ≤ 1 s, kesto ≤ ikkuna − 1.2 s), joten jokainen partikkeli ehtii pudota loppuun ennen unmounttia.
- Teematason animaatiotokeni `animFold` ei koskaan päädy CSS-muuttujaksi: `ThemeCSSVars` sisältää `animFold`:n ja `basic.tsx` tarjoaa sille arvon (`450ms`), mutta `ThemeApplier.tsx`:n `CSS_VAR_MAP` ei mäppää sitä `--ftp-anim-fold`-muuttujaan (vrt. muut anim-tokenit). Näkyy tyyppivirheenä (`CSS_VAR_MAP` puuttuu `animFold`), jota vite-build ei huomaa (ei tyyppitarkistusta). Vaikutus: teeman `animFold`-override ei vaikuta mihinkään; `--ftp-anim-fold` jää CSS-defaultiin (600ms). Havaittu bad beat -korjauksen yhteydessä, ei liity siihen — jätetty korjaamatta (erillinen bugi). Korjaus: lisää `animFold: '--ftp-anim-fold'` `CSS_VAR_MAP`:iin.
-[DONE] Bad beat -tärinässä tappiollisen pelaajan avatar sinkoutui pois omalta paikaltaan (n. puoli avatarin kokoa ylävasemmalle) sen sijaan että olisi tärissyt paikallaan. Juurisyy: istuinkääre keskitetään Tailwind v4:n `-translate-x-1/2 -translate-y-1/2`-luokilla, jotka Tailwind v4:ssä tuottavat CSS:n **`translate`-propertyn** (`translate: -50% -50%`) — eri kanava kuin `transform`. `bad-beat-shake`-keyframe animoi `transform`ia ja toisti `translate(calc(-50% + Npx), ...)` joka framessa, jolloin `-50%` pinoutui `translate`-propertyn päälle → yhteensä -100% → avatar hyppäsi ylävasemmalle ja tärisi siellä. (Räjähdys/teksti eivät kärsi tästä, koska ne käyttävät inline-`transform`ia, eivät Tailwindin `translate`-propertya.) Korjattu: `bad-beat-shake`-keyframe animoi nyt vain jitter-deltat (`translate(4px, -2px)` jne., 0,0 lepotilassa) ilman `-50%`:a, jolloin se komponoituu oikein `translate`-keskityksen kanssa → avatar tärisee paikallaan. Regressiovahti: `anim-translate-centering.test.ts` varmistaa ettei keyframe sisällä `-50%`:a (CSS-animaatiota ei voi ajaa jsdomissa). Verifioitu selaimessa computed-style + CSSOM -tarkastuksella.
-[DONE] Sama Tailwind v4 -luokan bugi kahdessa muussa animaatiossa (löydetty bad beat -tärinän jälkeen luokka-sweepissä): (1) `allin-label-appear` — "ALL IN" -lappu on keskitetty `left-1/2 -translate-x-1/2`:llä (`translate`-property), mutta keyframe piti `transform: translate(-50%, 0) ...` ja `forwards`-fill → lappu jäi PYSYVÄSTI siirtyneeksi koko leveytensä vasemmalle. (2) `live-action-pop` — live-pöydän toimintomerkki keskitetty `-translate-x-1/2 -translate-y-1/2`:llä, keyframe piti `transform: translate(-50%,-50%) scale()` ja `both`-fill → merkki jäi puoli kokoaan ylävasemmalle. Molempien juurisyy sama kuin bad beat -tärinässä: Tailwind v4:n `-translate-*` siirtyi `transform`ista `translate`-propertyyn, joten keyframe joka toistaa `-50%`:n `transform`issa tuplaa keskityksen. Korjattu: molemmat keyframet animoivat nyt vain `scale()`/`rotate()` ilman `translate(-50%...)`. Verifioitu selaimessa (settled `transform`-translaatio = 0,0, keskitys `translate`-propertyssä). Regressiovahti kattaa kaikki kolme keyframea.
